To app

1. /Users/piyush/Downloads/my-personal-application/mobile -> npm run server
2. /Users/piyush/Downloads/my-personal-application/mobile -> npm start
3. /Users/piyush/Downloads/my-personal-application/mobile -> npm run android -- --no-packager


# 1) Install Java 17
brew install openjdk@17
echo 'export JAVA_HOME=$(/usr/libexec/java_home -v 17)' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc

# 2) Set Android SDK env
echo 'export ANDROID_HOME=$HOME/Library/Android/sdk' >> ~/.zshrc
echo 'export PATH=$PATH:$ANDROID_HOME/emulator:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin' >> ~/.zshrc

source ~/.zshrc

to build android
-> ./android/gradlew -p android assembleRelease

--------------
Manual trigger if needed:

cd /opt/mobile/mobile-web-application
npm run release:android:auto



-----

git -c autoRelease.skipBuild=true pull origin main

sudo htpasswd -c /etc/phpmyadmin/.htpasswd dayal.electronics